global:
  domain: argocd.austinbayley.co.uk

configs:
  params:
    server.insecure: true

  cm:
    dex.config: |
      connectors:
        - type: github
          id: github
          name: GitHub
          config:
            clientID: Iv23lidURGmEy1jsqZ1z
            clientSecret: $github-client-secret:value
            orgs:
              - name: abayleyportfolio

  rbac:
    policy.default: role:readonly
    policy.csv: |
      g, abayleyportfolio, role:admin
