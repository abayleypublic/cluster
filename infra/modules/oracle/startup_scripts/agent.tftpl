#!/bin/bash

systemctl disable firewalld --now

curl -sfL https://get.k3s.io | INSTALL_K3S_VERSION=v1.34.1+k3s1 K3S_URL=https://${server_ip}:6443 K3S_TOKEN=${k3s_token} sh -s - agent \
  --node-name $(hostname)